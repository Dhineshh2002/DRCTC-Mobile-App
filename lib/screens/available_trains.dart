import 'package:flutter/material.dart';

class AvailableTrains extends StatelessWidget {
  const AvailableTrains({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
